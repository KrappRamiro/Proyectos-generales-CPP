#ifndef MATH_UTILS
#define MATH_UTILS
struct Rectangle {
	double length;
	double width;
};

double area (double length, double width);


double pow (double number, int pow=2);


double sum (double num1, double num2);
#endif
